// @thenexus/world-engine render — PixiJS v8 presentation layer.
// Requires a browser canvas/WebGL context. Compile-checked by typecheck;
// smoke-tested through the desktop app (no Node tests per Phase 4 plan).

export { WorldRenderer, type RendererOptions, type RendererPerf } from './render/world-renderer';
